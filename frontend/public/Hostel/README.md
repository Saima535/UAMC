Place Hostel page images in this folder and keep the filenames below so the page updates automatically.

Expected files:

- `hero-strip-placeholder.svg`

You can replace the SVG placeholder with the final image later. If you switch file extensions, update the Hostel page route accordingly.
