Place Publications page assets in this folder and keep the filenames below so the page updates automatically.

Expected files:

- `hero-strip-placeholder.svg`

You can replace the SVG placeholder with the final asset later. If you switch file extensions, update the Publications page route accordingly.
