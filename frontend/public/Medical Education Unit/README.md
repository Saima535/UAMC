Place Medical Education Unit page assets in this folder and keep the filenames below so the page updates automatically.

Expected files:

- `hero-strip-placeholder.svg`
- `main-meu-placeholder.svg`

You can replace the SVG placeholders with real image files later. If you switch file extensions, update the Medical Education Unit page route accordingly.
